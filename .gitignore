# Build artifacts
build/
dist/
*.o
*.a
*.so
*.dylib
*.dll
*.exe

# CMake — artefactos de build (no ignorar cmake/*.cmake del proyecto)
CMakeFiles/
CMakeCache.txt
cmake_install.cmake
CTestTestfile.cmake
Makefile

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Data and results (corpus grandes — ignorados)
data/*.fmd
data/*.fmr
data/*.bed
results/*.json
results/*.csv
timing.txt
# Corpus reales (deben ignorarse por tamaño)
data/real/
data/Escherichia_coli*
data/cere*
data/para*
data/influenza*
# Datasets sintéticos generados (grandes, reproducibles con scripts)
data/synthetic_1mb/
data/scaling/
data/classify/
data/bench_100mb_mutation_sweep/
# Resultados de benchmarks (regenerables; índices binarios pesados).
# Se ignora el contenido pesado pero se versionan los .md curados (análisis/hallazgos).
results/*
!results/*.md

# Caché sdsl-lite (generada por sr-index benchmarks en cwd)
*.sdsl
construction-*.json
construction-*.html
data/*/sr_build/
data/**/*.fmr

# Temporary
tmp/
*.tmp
*.temp


docs/memoria
CLAUDE.md
